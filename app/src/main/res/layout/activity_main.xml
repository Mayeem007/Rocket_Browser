<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- WebView fills the screen, with padding at the bottom for URL bar -->
    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="56dp" />

    <!-- URL bar at bottom -->
    <LinearLayout
        android:id="@+id/url_bar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:background="?attr/colorSurface"
        android:gravity="center_vertical"
        >

        <EditText
            android:id="@+id/url_input"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:hint="Enter URL or search"
            android:inputType="textUri"
            android:padding="8dp"
            android:singleLine="true"
            android:textColor="@android:color/black"
            android:textColorHint="@color/secondary"
            />

        <!-- Loading spinner (hidden unless loading) -->
        <ProgressBar
            android:id="@+id/url_loading_spinner"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="4dp"
            android:indeterminate="true"
            android:visibility="gone"
            style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Small"/>

        <!-- Refresh button (only visible when loaded) -->
        <ImageButton
            android:id="@+id/url_refresh"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/transparent"
            android:padding="8dp"
            android:src="@drawable/ic_refresh"
            android:contentDescription="Refresh"
            android:visibility="gone"
            android:tint="?attr/colorPrimary"/>

        <!-- You may want to also add a Go/FAB, but in modern UI, Enter key is enough -->
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
